# Environment Variables Configuration
# Copy this file to .env and fill in your values

# ===========================================
# GENERAL SETTINGS
# ===========================================

# The public URL of your blog (used in newsletter emails)
PUBLIC_URL=http://localhost:4173

# ===========================================
# EMAIL / NEWSLETTER CONFIGURATION
# ===========================================
# These are required for sending newsletters to subscribers

# SMTP Host (e.g., smtp.gmail.com, smtp.mailgun.org)
SMTP_HOST=smtp.gmail.com

# SMTP Port (typically 587 for TLS, 465 for SSL)
SMTP_PORT=587

# Set to 'true' for SSL (port 465), 'false' for TLS (port 587)
SMTP_SECURE=false

# Your SMTP username (often your email address)
SMTP_USER=this.enable@gmail.com

# Your SMTP password or app-specific password
# For Gmail: Generate an App Password at https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password

# ===========================================
# OPTIONAL: Blog Name Override
# ===========================================
# This can also be set in the Admin Panel > Settings
# Environment variable takes precedence if set

# BLOG_NAME=ExtraMele
